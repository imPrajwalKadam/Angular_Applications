import { AthsDirectiveDirective } from './aths-directive.directive';

describe('AthsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AthsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
